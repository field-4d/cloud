/*
 * testQuery.ts
 * Utility script to test BigQuery access from the frontend (for development only).
 *
 * TODO: This file requires './utils/queryBigQuery', which is missing. Restore or implement if needed.
 */

// import { queryBigQuery } from './utils/queryBigQuery';

/**
 * testBigQuery
 * Runs a test query against BigQuery and logs results.
 * Side effect: logs to console
 */
// async function testBigQuery() {
//   try {
//     const query = "SELECT email FROM `iucc-f4d.user_device_permission.user_table` LIMIT 10";
//     console.log('Executing query:', query);
//     
//     const results = await queryBigQuery(query);
//     console.log('Query results:', results);
//   } catch (error) {
//     console.error('Test failed:', error);
//   }
// }

// Run the test
// TODO: Remove or disable in production
// testBigQuery(); 